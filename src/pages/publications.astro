---
import Layout from '../layouts/Layout.astro';
import KnobNav from '../components/KnobNav.astro';
---

<Layout title="Publications — Emma Bennett" theme="home">
  <main class="wrap stub">
    <KnobNav active="publications" />
    <div class="stub-body">
      <h1>Coming later…</h1>
      <p>
        My publications will live here — set against Mizzou's Columns — with links
        out to my Google Scholar and ORCID profiles. Building it is on the
        roadmap.
      </p>
    </div>
  </main>
</Layout>

<style>
  .stub {
    min-height: 100vh;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-5);
    align-items: center;
    padding-top: var(--space-6);
    padding-bottom: var(--space-6);
  }
  .stub-body {
    max-width: 30rem;
  }
  .stub-body h1 {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--accent-bright);
  }
  .stub-body p {
    color: var(--ink-dim);
  }
  @media (max-width: 760px) {
    .stub {
      grid-template-columns: 1fr;
      justify-items: center;
      text-align: center;
    }
  }
</style>
