// @ts-check
import { defineConfig } from 'astro/config';

// Deployed to GitHub Pages at https://enbennett.github.io/website/
export default defineConfig({
  site: 'https://enbennett.github.io',
  base: '/website',
});
